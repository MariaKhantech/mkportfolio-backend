[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
litestar = "*"
asyncpg = "*"  # Assuming compatibility with Amazon Linux
ormar = "*"
uvicorn = "*"  # For local development; not used in Lambda but useful for API testing
boto3 = "*"  # AWS SDK for Python
aws-lambda-powertools = "*"  # Helpful utilities for Lambda functions

[dev-packages]
pytest = "*"  # For local testing

[requires]
python_version = "3.8.18"  # Adjust to match Lambda's supported runtime version
